# Copy to `.env` for local ergonomics — never commit real secrets.
SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/gatewayobs
SPRING_DATASOURCE_USERNAME=gatewayobs
SPRING_DATASOURCE_PASSWORD=gatewayobs_local

SPRING_KAFKA_BOOTSTRAP_SERVERS=localhost:9092
SPRING_ELASTICSEARCH_URIS=http://localhost:9200

REDIS_HOST=localhost
